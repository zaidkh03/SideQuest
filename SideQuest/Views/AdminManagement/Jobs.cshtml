@model AdminJobsPageViewModel

<partial name="_PageHeader" model="@(new PageHeaderViewModel
{
    Eyebrow = "Admin Portal",
    Title = "Jobs",
    Subtitle = "Monitor all quest records across statuses, categories, and companies."
})" />

<form class="sq-panel sq-filter-bar" method="get">
    <div class="sq-field">
        <label for="search">Search</label>
        <input id="search" name="search" value="@Model.Search" />
    </div>
    <div class="sq-field">
        <label for="status">Status</label>
        <select id="status" name="status">
            <option value="">Any status</option>
            @foreach (var status in Enum.GetValues<JobStatus>())
            {
                <option value="@status" selected="@(Model.Status == status)">@status</option>
            }
        </select>
    </div>
    <div class="sq-field">
        <label for="budgetType">Budget</label>
        <select id="budgetType" name="budgetType">
            <option value="">Any budget</option>
            @foreach (var budgetType in Enum.GetValues<BudgetType>())
            {
                <option value="@budgetType" selected="@(Model.BudgetType == budgetType)">@budgetType</option>
            }
        </select>
    </div>
    <div class="sq-form-actions sq-form-actions--inline">
        <button class="sq-button sq-button--primary" type="submit">Filter</button>
        <a class="sq-button sq-button--ghost" asp-action="Jobs">Reset</a>
    </div>
</form>

<section class="sq-panel">
    <div class="sq-table-wrap">
        <table class="sq-table">
            <thead>
                <tr>
                    <th>Quest</th>
                    <th>Company</th>
                    <th>Reward</th>
                    <th>Status</th>
                    <th>Workers</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var job in Model.Jobs)
                {
                    <tr>
                        <td>
                            <strong>@job.Title</strong>
                            <span>@job.CategoryName</span>
                        </td>
                        <td>@job.CompanyName</td>
                        <td>@job.RewardLabel</td>
                        <td><span class="sq-status @(job.Status == JobStatus.Completed ? "is-success" : job.Status == JobStatus.Cancelled ? "is-danger" : "")">@job.Status</span></td>
                        <td>@job.AcceptedWorkers/@job.WorkersNeeded</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>
