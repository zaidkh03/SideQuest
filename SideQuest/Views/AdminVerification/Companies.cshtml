@model VerificationListPageViewModel

<partial name="_PageHeader" model="@(new PageHeaderViewModel
{
    Eyebrow = "Admin Review",
    Title = "Company verification queue",
    Subtitle = "Search and review company onboarding requests across every verification status.",
    ActionText = "Back to Summary",
    ActionUrl = Url.Action("Index", "AdminVerification"),
    ActionIcon = "arrow_back"
})" />

<form class="sq-panel sq-filter-bar" method="get">
    <div class="sq-field">
        <label for="search">Search</label>
        <input id="search" name="search" value="@Model.Search" />
    </div>
    <div class="sq-field">
        <label for="status">Status</label>
        <select id="status" name="status">
            <option value="">Any status</option>
            @foreach (var status in Enum.GetValues<VerificationStatus>())
            {
                <option value="@status" selected="@(Model.Status == status)">@status</option>
            }
        </select>
    </div>
    <div class="sq-form-actions sq-form-actions--inline">
        <button class="sq-button sq-button--primary" type="submit">Filter</button>
        <a class="sq-button sq-button--ghost" asp-action="Companies">Reset</a>
    </div>
</form>

<section class="sq-panel">
    @if (Model.Companies.Count == 0)
    {
        <div class="sq-compact-empty">No company requests match this filter.</div>
    }
    else
    {
        <div class="sq-table-wrap">
            <table class="sq-table">
                <thead>
                    <tr>
                        <th>Company</th>
                        <th>Owner</th>
                        <th>Registration</th>
                        <th>Status</th>
                        <th>Submitted</th>
                        <th>Reviewed</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var company in Model.Companies)
                    {
                        <tr>
                            <td><strong>@company.CompanyName</strong><span>@company.Location</span></td>
                            <td><strong>@company.UserName</strong><span>@company.Email</span></td>
                            <td><span class="sq-status">@company.RegistrationNumber</span></td>
                            <td><span class="sq-status @(company.Status == VerificationStatus.Approved ? "is-success" : company.Status == VerificationStatus.Rejected ? "is-danger" : "")">@company.Status</span></td>
                            <td>@(company.SubmittedAt?.ToLocalTime().ToString("MMM d, yyyy") ?? "Not submitted")</td>
                            <td>@(company.ReviewedAt?.ToLocalTime().ToString("MMM d, yyyy") ?? "Not reviewed")</td>
                            <td><a class="sq-button sq-button--ghost sq-button--sm" asp-action="Company" asp-route-id="@company.ProfileId">Review</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>
